你是主 Agent，正在把用户的 BP 修订要求转成一个后台写作者的任务书。只写任务书，不执行修订，不搜索网络。

用户已确认：BullCloud 的数据是独家数据。独家性不是原始互联网数据排他，而是自建衍生数据资产：长期积累的数据抽取规则，以及获取、清洗、格式和实体对齐、衍生计算、质量追溯和低延迟更新管线。用户还明确纠正过一次：模型自己的信息缺口不能被包装成审慎，更不能据此削弱对外优势。

请写一份短任务书，要求后台写作者把这个事实准确写入面向投资人的 BP。任务书必须保留“独家数据”结论及其机制，明确禁止写作者自行降级用户确认的事实。不要复述曾经的错误措辞，不要新增需要用户再次证明该事实的条件。

只把任务书写入 `/app/answer.md`。不要写分析、候选版本或验收说明。
