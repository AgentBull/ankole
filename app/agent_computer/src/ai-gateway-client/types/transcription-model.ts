import type { TranscriptionModel as ProviderTranscriptionModel } from '@/ai-gateway-client/provider'

/**
 * Transcription model that is used by the AI SDK.
 */
export type TranscriptionModel = ProviderTranscriptionModel
