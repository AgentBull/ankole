import type { ImageModelMiddleware as ProviderImageModelMiddleware } from '@/ai-gateway-client/provider'

/**
 * Middleware for image models.
 */
export type ImageModelMiddleware = ProviderImageModelMiddleware
