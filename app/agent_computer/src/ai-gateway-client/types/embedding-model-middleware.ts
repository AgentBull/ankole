import type { EmbeddingModelMiddleware as ProviderEmbeddingModelMiddleware } from '@/ai-gateway-client/provider'

/**
 * Middleware for embedding models.
 */
export type EmbeddingModelMiddleware = ProviderEmbeddingModelMiddleware
