{
  "name": "@ankole/agent-computer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "dev": "sh -c 'echo \"Agent Computer worker runs only inside the Docker image; mount TS source into the container for edit/run feedback.\" >&2; exit 1'",
    "start": "sh -c 'echo \"Agent Computer worker runs only inside the Docker image; use the control-plane Docker worker e2e or docker run.\" >&2; exit 1'",
    "build": "bun build src/main.ts --target bun --outdir dist",
    "gen:proto": "bun scripts/gen-proto.ts",
    "gen:proto:check": "bun scripts/gen-proto.ts --check",
    "gen:rpc-contract": "bun scripts/gen-rpc-contract.ts",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "bun run fmt:check && bun run type-check",
    "//test": "Docker-only: Devkit resolves the published or content-addressed Worker image, consumes the control-plane container bootstrap contract, then adds the package source/test mounts and Bun command. For full runtime coverage use the control-plane Docker worker e2e.",
    "test": "cd ../.. && bun run kit agent-computer-test --suite unit",
    "test:integration": "cd ../.. && bun run kit agent-computer-test --suite integration"
  },
  "dependencies": {
    "@agentbull/active-support": "^1.6.0",
    "@ankole/browser": "workspace:*",
    "@ankole/kernel": "workspace:*",
    "@bufbuild/protobuf": "2.14.0",
    "@opentelemetry/api": "^1.9.1",
    "@opentelemetry/otlp-transformer": "^0.221.0",
    "@opentelemetry/resources": "^2.10.0",
    "@opentelemetry/sdk-trace-base": "^2.10.0",
    "openai": "^7.4.0",
    "pino": "^10.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@bufbuild/buf": "1.72.0",
    "@bufbuild/protoc-gen-es": "2.14.0",
    "@types/bun": "latest"
  }
}
