# Deep Research Job Project

This directory is the private project for one BackgroundAgentJob. Treat every
`workspaces/<mount-id>` entry as an explicit external mount and write only to
mounts declared read-write by the task context. Keep Plugin templates, Codex
configuration, and research scaffolding in this private project; never copy
them into a mounted source repository.
